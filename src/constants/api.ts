const BASE_URL = 'https://backend.wahanavirendra.id/api';

export const API_RUTE = {
    RUTE_LIST: `${BASE_URL}/dermaga`
};

export const API_JADWAL = {
    GET_JADWAL: `${BASE_URL}/penjualan/cari-jadwal-non-token`
}